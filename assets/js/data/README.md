# 数据目录

- `characters.js`：人物基础资料、CV、所属阵营和出场信息。
- `relations.js`：人物之间的关系、类型、标签与说明。
- `factions.js`：阵营、关系类型颜色和阵营背景图片。
- `works.js`：动画、OVA、音乐、小说、漫画和游戏作品。
- `resources.js`：图像资料分类及视频、音频、文本外链。

`../data.js` 是统一入口。页面仍通过 `src/data.ts` 读取数据，新增或修改资料时只需编辑对应领域文件。作品与资源条目均为独立对象，不使用批量生成器，字段修改不会联动其他条目。
